{# include/dialogFenster Twig template #}
<div class="modal fade" id="{{ ope }}{{ id }}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">{{ ope|capitalize  }} Fenster</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
          <span aria-hidden="true"></span>
        </button>
      </div>
      <div class="modal-body">
        <p>Möchten Sie den Datensatz mit ID {{ id }} {{ opd }}? </p>
      </div>
      <div class="modal-footer">
        <a class="btn btn-primary" href="{{ ope }}/{{ id }}">Ja</a>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Nein</button>
      </div>
    </div>
  </div>
</div>
